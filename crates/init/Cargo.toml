[package]
name = "init"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
common = { path = "../common" }
mlua = { version = "0.8.8", features = ["luajit"] }
nvim-oxi = { version = "0.2.2", features = ["neovim-nightly", "mlua"] }
